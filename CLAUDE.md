# Project guidance

Read `AGENTS.md` for commands, account-protection invariants and change requirements.
Read `docs/architecture.md` for module responsibilities, compatibility and offline tests.

The Python entry point is `server.py`; runtime implementation is in `nwafu_proxy/`.
